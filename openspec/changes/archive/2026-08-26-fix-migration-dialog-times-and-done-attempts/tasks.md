## 1. Read the times the backend actually sends

- [x] 1.1 Pass the per-reason times into the normalizer the way the occurrence count already is: in `errorEntries`, read `firstOccurredAts` / `lastOccurredAts` defensively (array or empty) and hand `last[i]` and `first[i]` to `normalizeErrorMessage` alongside `counts[i]`; verify by reading the call back and confirming the three parallel arrays are indexed with the same `i` as the reason
- [x] 1.2 Fall back to those values in `normalizeErrorMessage` for both shapes — the plain-string branch and the object branch — keeping last-occurrence-before-first order; verify the object branch still prefers a time carried inside the entry over the fallback
- [x] 1.3 Verify against the live payload that a `null` array element yields no time rather than a rendered placeholder, and that a node with one timed and one untimed reason shows each correctly — **verified 2026-08-26** by running the shipped `errorEntries` / `normalizeErrorMessage` / `errorMessageLine` functions, extracted verbatim from the SFC, against the live `babylon5` payload. Pending node 34 (legacy `NullPointerException`, `firstOccurredAts`/`lastOccurredAts` both `[null]`) renders reason text alone, no time and no placeholder. A synthetic node mixing an untimed and a timed collapsed reason renders `old reason` and `<10:05:00>: new reason (3×)` — the collapsed entry taking its *last* occurrence. The anticipated object shape still wins over the arrays, and a pre-change payload with no time arrays at all still renders.

## 2. Show a done node's attempt count

- [x] 2.1 Drop the `column === 'done'` exclusion from `attemptCount()` and update the comment that justified it to the backend's current rule (the count includes the successful attempt); verify a done node with a count shows the line and a done node without one omits it

## 3. Verify

- [x] 3.1 Run the project's lint and unit tests and verify they pass — `npm run lint` and `npm run build` both run clean for this change: **zero lint findings of any kind in `KioskMigrations.vue`**, and `Build complete`. The repo has no unit-test harness (`package.json` has only `serve`/`build`/`lint`), so lint plus build is the full suite. The 10 pre-existing `camelcase` errors and 38 warnings all come from other files (`Floorplan.vue`, the `*Service.ts` `any` returns) and are unchanged by this change.
- [x] 3.2 Verify against the live `GET /reconciliation` from `babylon5`: error node 113's two reasons render with their times 33 seconds apart, done node 161 "Server" shows both its migration time and an attempt count of 1, and a pre-deploy done node (e.g. 32 "Licht Essen/Wohnzimmer") renders with neither line and no placeholder — **all three verified 2026-08-26** through the same extracted-function harness. 113: `10:15:30.763` and `10:16:03.646`, the 33-second span, one per reason. 161: `migrationTime 2026-08-26T10:13:07.921`, `attemptCount 1`. 32: `migrationTime null`, `attemptCount null`, no reason lines — both lines simply absent. Pending node 143 also checked: its post-deploy reason renders with its time while its `attemptCount` of 0 still renders as 0, an unreachable attempt not having consumed the budget.
- [x] 3.3 Deploy and confirm at the kiosk, closing `java-overmind-server`'s task 3.3 of `reconciliation-node-times`: open a done node and an error node with several reasons and confirm both times render, and that a node whose times are unknown still renders without placeholders. Needs a person at the kiosk — **confirmed at the kiosk 2026-08-26** against the deployed build, all four checks: error node 113's two reasons each carry their own time, `10:15:30` and `10:16:03`, the 33-second span visible on the display; done node 161 "Server" shows `Migriert am: 26.08.2026 10:13` and `Versuche: 1`; done node 32 "Licht Essen/Wohnzimmer" ends after its appliance block with neither line and no placeholder; pending node 34's untimed legacy `NullPointerException` renders as reason text alone. Closes `reconciliation-node-times` task 3.3.
